type Query {
  getRSVPByEmail(email: String!): RSVP
}

type RSVP @model @auth(rules: [{ allow: public }]) {
  id: ID!
  name: String!
  email: String!
  attending: Boolean!
  guests: Int
}
input AMPLIFY {
  globalAuthRule: AuthRule = { allow: public }
}